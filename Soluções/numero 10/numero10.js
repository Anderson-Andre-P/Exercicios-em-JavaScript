// Faça um programa que leia o valor do raio de um círculo e calcule a área desse círculo. Para esse cálculo, use: 𝐴=𝜋𝑟^2. Onde 𝜋 vale: 3.141592

const raio = Number(prompt('Digite o valor do raio'));

alert(`Área = ${Math.PI * (raio **2)}`);
