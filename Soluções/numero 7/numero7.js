// Faça um programa que receba a base e a altura de um triângulo e imprima a sua área.

const base = Number(prompt('Digite o tamanho da base do triângulo'));
const altura = Number(prompt('Digite o tamanho da altura do triângulo'));

alert(`(${base} x ${altura}) / 2 = ${(base * altura) / 2}`);
